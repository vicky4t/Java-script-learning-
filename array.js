// we study on array most imp topic 
// lets understand some concept and methods ...

// let arr = [23,4,79,68,7,81,9]; /// this the definde array 

// arr.push(30);   /// this method (push();) it add the value in array at the end of array
// arr.pop();     // this method (pop();) it is remove the value from array at the end of array

// arr.shift(); /// this method remove value at the starting point of arry 
// arr.unshift(88); // this method add the value at the start of array

// arr.slice(0,4);   // this method can copy the main array in the value we give us that is the indexing we give to take the value from to that.

// arr.splice(2,5);  // this method remove the values in array by indexing (2) to (5) removes values

// let sortedArr = arr.sort(function(a,b){   /// this method use to sort the array in form of decending and ecending order
    // return a - b;  ///acending order (b-a) is decending order
// })

// console.log(arr);




let arrr = [1,2,3,4,5,6,7,8,9];

// arrr.forEach(function(val){  /// foreach matlb har ek ke liye / or har ek ke liye function chalenga 
//     console.log(val + 100);
// })

